NEXT_PUBLIC_INDEXER_URL=http://127.0.0.1:3001

# Required in deployed environments to enable WalletConnect and mobile wallets.
# Local development falls back to an injected browser wallet when omitted.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
